// Optimized

export default {
    setTest(state, data) {
        state.test = data;
    },
    thisStepMut(state, param) {
        state.thisStep = param;
    },
    checkoutStepsMut(state, param) {
        state.checkoutSteps = param;
    },
};
